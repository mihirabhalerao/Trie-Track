package com.example.trietrack.model.enums;

public enum HelpLevel {
    SOLO,       // Solved completely on your own with zero help
    HINT,       // Needed 1-2 minor hints, but wrote the code yourself
    SOLUTION    // Had to read the full solution or copy an algorithmic breakdown
}
